{{> header}}

{{#if showList}}
  Repositories!
  <ul>
    {{#each repos}}
      <li>{{name}}</li>
    {{/each}}
  </ul>
  Ok, now you can <a href="/">hide the list</a>.
{{else}}
  <a href="/?showlist=true">Show me the list</a>, please.
{{/if}}
