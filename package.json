{
  "name": "challenges",
  "scripts": {
    "lint": "./tools/lint",
    "lint:fix": "npm run lint -- --fix",
    "format": "prettier --write 'packages/**/*.js' 'packages/**/*.jsx' 'packages/**/*.tsx' 'packages/**/*.ts'"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {},
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "dependencies": {
    "colorette": "^2.0.19",
    "dotenv": "^16.0.0"
  }
}
