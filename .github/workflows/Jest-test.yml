name: Tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - run: npm install
    - run: npm jest
      # uses: stefanoeb/jest-action@1.0.3
      # with:
      #   jestArgs: path/to/client/tests/dummy.test.js
