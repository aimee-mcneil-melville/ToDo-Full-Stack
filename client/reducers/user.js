const initialState = {
  username: '',
  isAdmin: false,
  gardenId: null
}

const user = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default user
